# Lachie brief triage notes (verifiers at main 84c4ae0, 2026-08-25)

## P0 (orchestrator, executed)
- sdk.d.ts 0.3.x: PostToolUse input field is `tool_response` (2143, 2168); `updatedToolOutput` at 2182; `tool_output` absent from SDK types.
- harness reads `input.tool_output` (session.ts:974-1000, types.ts:95); test fake fabricates `tool_output` (session.test.ts:115).
- REAL TRAFFIC: repo .harness/telemetry.db has 6 post-tool tool-trace rows (5 Bash, 1 Read; runs 2026-07-27 and 2026-08-07), all ALLOWED (6 pre-tool + 6 post-tool hook-fired, 0 denies), resultSummary null on 6/6. RUNTIME CONFIRMED, no API spend.
- Agent A report pending (types, cast, R-4 text, #80 branch).

## Agent C (P1.4-P1.6)
- P1.4 MemoryStore.delete: CONFIRMED executed; understated: {type,limit:0} read()=0 rows but delete()=3; includeStale/order/limit all ignored; unknown field silently ignored. No CLI caller; on public surface (index.ts export, ADR-0023). Docs-parity extra: process/devlog/week-5.md:24-33 claims read/delete share one matcher ("closes the class"): false. Fix: narrow delete's filter type or reject fields; ADR-0009 line; week-5 correction.
- P1.5 router: PARTIAL. True for `run` (flags: prompt, --skills-dir, --db, --max-turns only; DEFAULT_DESCRIPTOR build/low/4000 -> claude-sonnet-5 every time). FALSE for the CLI as a whole: eval routes per-task descriptors (task.ts:22,37,215; runner.ts:380; init template lookup->haiku). Gap already recorded process/devlog/week-6.md:52-53. No safety consequence (under-routed, never mis-routed). Fix: --shape/--sensitivity/--expected-tokens on run.
- P1.6 corpus numbers: CONFIRMED. Shipped: 53 cases, 41 malicious, 37 detected (90.2%), 23 blocked, 14 asked, 4 missed; changed at db164e6 (PR #47, 2026-07-28, 51->53). Stale present-tense: docs/eval-methodology.md:63,227 (+73-75 dated but 40-denominator), docs/security-model.md:414,436 (+419 dated), docs/blog/adversarial-evaluation.md:16. ADR-0018 is a dated record (fine). README says ">=50" (true). No gate covers corpus numbers (check-docs = structure + ADR count; check-test-count = README Tests row). Fix: re-derive prose + new gate vs baseline.json meta/totals.

## Agent B (P1.1-P1.3)
- P1.1 ask->deny: PARTIAL. Mechanics CONFIRMED (evaluate.ts:272-274 throws PermissionDenied "'ask' with no prompter configured"; cli.ts:451 registers permissionHook with no prompter). "Silently" REFUTED executed: composeSecurity (cli/shared.ts:178-188) pushes startup warning "settings contain 'ask' permissions but no prompter is configured; 'ask' will deny (ADR-0014 §4)" -> stderr (cli.ts:415-417), pinned cli.test.ts:975-990; per-deny reason carries the suffix into telemetry/model/memory. Documented: ADR-0014:28 ("CLI does not yet wire a TTY prompter... deferred until interactive mode"), :74 revisit-if; architecture.md:84; security-model.md:344-345. README has no settings schema at all (only gap). Severity LOW. Fix if wanted: cli.ts:451 prompter when stdin.isTTY, new cli/prompter.ts, ADR-0014 §4.
- P1.2 typos: typo half CONFIRMED executed: unknown KEYS at permissions/sandbox/root level silently ignored (permissions/settings.ts:72 destructure; sandbox/settings.ts:55), composeSecurity(typos) -> open posture, warnings=[]; defaultDecison typo -> evaluate = allow; sandbox.path typo -> Write /etc/passwd allowed. Values and nested keys DO fail loud (denny, decison, alow all throw). Permissions unknown-key ignore is BLESSED by settings.test.ts:32-37 (futureKnob) + ADR-0014 §5 "unknown siblings ignored" (root-level meaning); sandbox parser CONTRADICTS its own comment (sandbox/settings.ts:40-43 "never skipped... would fail open") and has no unknown-key test. No security-model row for typos. Severity MEDIUM. Composition half: CONFIRMED behaviour (settings.ts:126 project ?? user) but it is R-8 (security-model.md:371, High for hardened users, deliberate per ADR-0014 §5:32; :80-83; :455) so NOT a new hole; however types.ts:17-21 blanket tighten-only sentence is imprecise (scalar exempt). Fix: reject/warn unknown keys inside permissions+sandbox (keep root siblings ignored), flip test :32-37, sandbox test, comment fix, security-model row; composition = decision (leave, or amend types.ts sentence).
- P1.3 tool table: CONFIRMED executed and understated: NOT drift, never complete. Pinned 0.3.201 unchanged since e872f63 (2026-07-06). sdk-tools.d.ts has 40 *Input; path/command-bearing NOT in table: Projects(path,local_path), Workflow(scriptPath), Monitor(command), Artifact(file_path), EnterWorktree(path). MultiEdit in table but absent from SDK types. Executed: Artifact/EnterWorktree/Projects/Monitor/Workflow with /etc paths or rm -rf all PASS both gates (wildcard deny fails: evaluate.ts:97-98 JSON.stringify fallback starts with '{"'); sandbox.ts:205-206 unknown tool -> return. defaultDecision:deny closes permissions half only. Pin test tool-targets.test.ts:9-18 pins list to itself; no SDK-diff gate. R-9 (security-model.md:372) describes mechanism as accepted residual but as a FUTURE "new tool"; ADR-0015 §2 (:22) "covers all path/command-taking SDK tools" is FALSE at pinned version = docs-parity defect. UNCHECKED: whether headless query() exposes these 5 tools (session.ts:1036-1046 passes no tools/allowedTools -> SDK default; claude binary 2.1.245 contains the strings). Aside: sdk.mjs filePatternTools lists NotebookRead, Cd (absent from table AND sdk-tools.d.ts). Severity MEDIUM (R-9's own rating). Fix: 5 table entries (Projects has 2 fields; optional fields -> ToolTarget shape decision), derived test parsing sdk-tools.d.ts (prototype verify-p1a/sdk-diff.ts, 20 lines), R-9 wording "any" not "new", ADR-0015:22 correction, MultiEdit annotate/drop.

## Agent E (P2 premises + product gaps) — fidelity only
- P2.1 verifier: CONFIRMED (5 enums types.ts:6-8; no prose/severity/evidence; ADR-0020:524-528 "un-investigatable without a live re-run" verbatim). DELIBERATE + PRICED: :67-76 prose findings would downgrade E-1's structural no-leak invariant to procedural; confidence cut :591-595 (no calibration story); revisit-if :599-616. => a design decision with a recorded revisit trigger, not a gap.
- P2.2 injection: PARTIAL. 15 rules (rules.ts:16-122) + 2 unicode (scan.ts:57-58) + a strip-and-rescan pass the reviewer doesn't count (scan.ts:44-46,133-151). Judge = type-only seam (types.ts:43,52; scan.ts:87-88). ADR-0016:58-62 designs THREE modes off|suspicious|always; `always` escalates every non-block incl pass -> reviewer's "only ask" describes one mode. 4 misses all verdict pass (exfil-02, indirect-09, jailbreak-03, jailbreak-skill-header-spoof). ADR-0018:49-57 names the semantic gap. Trigger: ADR-0016:82-86 implement S-5 when heuristic rate <90%; live 90.24% (0.24 above). Recorded backlog: process/05-week-plan.md:82; security-model.md:453.
- P2.3 skills: PARTIAL. Budget 256_000 (session.ts:349), load order (:496-497), whole-skill drop, no catalogue/fetch tool, no approval path (ADR-0026:32 decision 8, cost named; revisit R1 :67). "Silently" REFUTED: stderr warn (session.ts:648-653,696-700; cli.ts:471), SessionResult.droppedSkills (types.ts:250,288-346), telemetry skill-drop rows (session.ts:755-758) — ADR-0026:34 decision 9, :51-53 closed #46. Named FPs confirmed ADR-0026:38-42 (shields.io badge; skill documenting injection) with test exhibits session.test.ts:1016,1387-1397. Carve-out omitted by reviewer: ADR-0026:24 decision 4 (all-hidden-unicode hits don't drop).
- P2.4 routing: thresholds CONFIRMED (table.ts:24,30; ADR-0007:60 "unseated defaults, not derived from measurement"; re-affirmed 2026-07-27 :91). "Untestable" PARTIAL: ADR-0007:80 says a router that CALLS A MODEL to pick a model is "non-deterministic, expensive, and untestable" (2026-05-20), not classification per se; never re-examined (ADR-0024 silent). No calibration backlog item.
- P2.5 oracle: CONFIRMED for the 2 eval/golden samples (pong; non-empty); PARTIAL for "no evaluator at all": examples/repo-qa has 2 oracles asserting specific facts (ADR-0017 named + R-10 caveat; sticky-deny merge rule, numTurns===1). ADR-0017:141-143 names self-report-only limitation; ADR-0020:25-31 names the verifier as the mechanism for incomplete/incorrect. Note: golden eval does NOT run in per-PR CI (05-week-plan.md:102; gates.yml runs redteam only) though ADR-0017:11-13 says "sample suite passing in CI" — minor parity point.
- Product gaps: (i) one prompt/run CONFIRMED, recorded ADR-0010:102-103 revisit v1.x (sdkSessionId captured session.ts:1050-1051 never fed back). (ii) ceilings: only maxTurns (types.ts:119-127; CLI default 10); $ budget = H-6 deferred v1.x (01-requirements.md:22); token/wall-clock/cancel NOT recorded anywhere. (iii) retry/timeout: zero hits; NOT recorded. (iv) OTel: E-5 deferred v1.x (01-requirements.md:43). (v) init refuses: deliberate ADR-0021 decisions 2, alternatives :120-121 rejected --force/merge.
- Cite note: tasks/2026-07-13-currency-review-synthesis.md does NOT exist in the repo (my brief's guess); backlog lives in process/01-requirements.md + 05-week-plan.md + ADR revisit-ifs.

## Live smoke 2026-08-25 (orchestrator, $0.1668, build e4191d5, scratch smoke-tools/)
- Prompt: call Bash once (echo), then list all tools. rc=0, 2 turns, denied=0.
- tool-trace: Bash post-tool resultSummary=null on a REAL allowed call, TODAY's build. hook-event: pre-tool + post-tool fired. => P0 runtime re-confirmed on current code.
- Model's own tool list (headless SDK mode): Agent, Bash, Edit, Read, ReportFindings, ScheduleWakeup, Skill, ToolSearch, Workflow, Write. => `Workflow` (scriptPath) IS exposed and passes both gates (B executed). Artifact/EnterWorktree/Monitor/Projects NOT exposed in this run (one sample; ToolSearch can surface deferred tools). Agent/Skill/ToolSearch/ScheduleWakeup/ReportFindings also outside the table (no path/command field except Skill args?). Glob/Grep/NotebookEdit/MultiEdit absent from the list (likely deferred behind ToolSearch).
- Closes B's UNCHECKED: P1.3 exposure CONFIRMED for Workflow.

## Agent A (P0)
- SDK 0.3.201 pinned since 3dfd598 (2026-07-06), never bumped. sdk.d.ts:2164-2173 PostToolUseHookInput has REQUIRED `tool_response`; no `tool_output` anywhere in SDK .d.ts; the bundled claude binary (2.1.201) builds the wire object with tool_response (16 hits, 0 tool_output); sdk.mjs handleHookCallbacks passes input through untouched. updatedToolOutput (2182, validated at runtime against the tool's output shape: "does not match ... output shape" string in binary); updatedInput on PreToolUse (2206). Both channels present in the ONLY SDK version the harness ever ran (tool_response+updatedInput since 0.1.0; updatedToolOutput since 0.2.140, 2026-05-12).
- Harness: no adapter; SdkHookInput (types.ts:91-97) has tool_output?; session.ts reads input.tool_output x5 (:974,980,981,983,1000). stringifyForScan(undefined)='' -> scan passes; hasOutput false -> redactor skipped, resultSummary null; custom post-tool hook gets result undefined. EXECUTED with real scan/redact: SDK-shaped event -> scanner [""], no redaction, resultSummary null, warnings []; harness-shaped event -> block + redaction. Pre-tool deny/input redaction, memory-summary redaction, skill channel UNAFFECTED.
- Types: SDK PostToolUseHookInput assigns to SdkHookInput (optional field + structural supertype), and a literal with tool_response is TS2353 REJECTED by the harness type: the type steered the fake to the wrong field. The cli.ts cast (418-425 on main) is not the enabler; the hook callback type-checks with no cast.
- Real traffic: 6/6 null in .harness/telemetry.db (8 sessions 07-14..08-07); 8/8 null in examples/repo-qa/.harness/telemetry.db-wal (string scan). handlersFired 0 on live post-tool rows (custom-handler sub-claim scripted-only).
- Test fake: session.test.ts:115 tool_output; docstring :74-77 "the way the real SDK would" is false; NO file in src/eval imports SDK types (only dynamic import() in cli.ts:418, eval-command.ts:145). 107/107 green.
- R-4: security-model.md:367 "No SDK result-rewrite channel exists yet" (added f5ae09b 2026-07-08); also :216,:313,:388,:396; architecture.md:77,264; README.md:25; blog/harness-not-framework.md:124; ADR-0012 §9:72; ADR-0013 §9:62-64 (justified from the harness's OWN SdkHookOutput type); ADR-0026 R4 revisit-if :71. TWIST: ADR-0010:88-92 (2026-07-06, same commit that pinned the SDK) says "Week 2 must either use the SDK's `updatedToolOutput` hook output or rework this seam" — internal contradiction, not SDK drift.
- #80 branch: no change to the defect (line drift +49 in session.ts only).
- Reviewer errors: symbol is SdkHookInput not PostToolInput; "redactor never fires" true for OUTPUT only; cast is not the enabler; custom-hook claim not live-observed; missed ADR-0010.
- Fix surface: types.ts SdkHookInput (tool_response, discriminate PostToolUse, or import SDK HookCallback/PostToolUseHookInput); session.ts postToolCallback 5 reads; test fake + SDK-shaped-literal test + type-parity probe; docs parity list above + telemetry/types.ts:46 comment. Enforcement via updatedToolOutput = separate design decision (shape validation, FP policy) — do NOT bundle with the field fix.

## Agent D (P1.7 a-f) — all executed, nothing UNCHECKED
- (a) PEM >16384: CONFIRMED + understated: boundary exactly 16,384 between-fence chars (16,382 body in wrapped block); above it whole block incl fences emitted raw; UNTERMINATED oversized also leaks. Existing test 9,600 chars. Docs FALSE: rules.ts:172 description "(terminated, unterminated, or oversized)"; ADR-0013:99-102 "always fully redacted". Bench: bounded regex 118ms on ADR's worst case vs `*?` + `|$` 0.2ms (the bound is both the leak and the slower variant; DoS rationale ADR-0013:109-116 describes the bounded behaviour). Exploit weight LOW (real keys <16KiB; attacker could base64), doc/test honesty MEDIUM.
- (b) stdout unredacted: CONFIRMED. session.ts:1063-1066 onText -> cli.ts:470 sanitizeForTerminal only; redactSecrets used for tool output + memory only. Executed: AKIA key reaches stdout. No doc claims stdout redacted (README:131 "before anything is retained"; R-4 "emit" ambiguous). Scope gap not parity defect. MEDIUM-LOW (CI log case is the real one). Fix: cli.ts:470 redact before sanitize, or session.ts:1064 at capture fail-closed; one doc sentence.
- (c) skills-root symlink: CONFIRMED, NOT an accepted residual. load() realpaths root and contains entries BELOW it only; project/skills -> outside dir loads silently (root recorded as symlink path); golden containSkillsDir refuses. Only skill-schema-valid files inject (payload = other skill packs, not arbitrary .md). ADR-0006:111 containment claim vacuous when root redirected; task.ts:93-96 / ADR-0017:40-44 name the gap and fix it for eval only; no R-row. MEDIUM-LOW. Fix: contain root vs cwd in load() (two-stage like containSkillsDir) or refuseSymlinkedDir (cli/shared.ts:63) on skillsDir at cli.ts:395-466; test; ADR-0006 sentence; R-row.
- (d) glob chars: PARTIAL. Reviewer's exploit REFUTED: argv0 exact canonical compare (sandbox.ts:141-145) so /bin/s? never matches /bin/sh; /bin/sh in SHELL_RUNNER_BINARIES denied even if allowlisted. TRUE narrower variant: a glob-shaped allowlist ENTRY (/bin/s?, /bin/*, /usr/local/bin/*) is accepted verbatim, passes isBlockedFirstToken, no startup warning, and the executing shell expands it to /bin/sh (bash+zsh executed). Falsifies ADR-0015:30 "does not change which program starts" and :32 "unconditional" blocklist. Operator misconfig (plausible: path dimension is prefix-based so /usr/local/bin/* reads like a dir allow). LOW. Fix: deny argv0 containing *?[ in allowCommand, and/or reject glob-shaped entries at parse; ADR-0015 §3; test.
- (e) settings loader envelope: CONFIRMED gap vs the project's own standard. settings path: no lstat, no ancestor walk, no O_NOFOLLOW, no byte cap, parse error embeds V8 snippet (10 chars of arbitrary file to stderr via cli.ts:409). baseline.ts:154-250 has all four + deliberate non-String(error). Executed: symlink -> outside JSON loaded silently; symlink -> AKIA file leaks 10 chars in error; 50MB loads (rss 95->326MB). security-model §2:48-50 classes project settings attacker-influenced; no doc claims file envelope. baseline.ts:149-152 says extract lstat guard "on a third consumer" = this. LOW-MEDIUM. Fix: composeSecurity (cli/shared.ts:146-173) lstat+size cap before readFile, hoist refuseSymlink/refuseAncestorSymlinks to src/internal/; settings.ts:43-44 drop detail; §3.1 sentence.
- (f) golden pack bounds: CONFIRMED but DOCUMENTED v1 revisit-if (ADR-0017:104-111, :147-151, :251-253). schema maxTurns {integer, minimum 1} no max; 10,000 tasks parse ok; no count cap; pre-flight count informational. ADR wording "default maxTurns cap" overstates (default != cap). LOW-MEDIUM (operator runs the pack). Fix: schema maximum; --max-tasks; close revisit-if; wording.
- Cite drift: cli.ts:392-393 -> :470-471; schema.json lives at src/eval/golden/schema.json.
