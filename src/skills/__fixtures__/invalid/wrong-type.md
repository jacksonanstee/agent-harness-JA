---
name: wrong-type-skill
description: A skill where trigger.keywords is a string instead of an array.
version: 1.0.0
trigger:
  keywords: "just-a-string"
---

# Wrong Type

This skill's `trigger.keywords` is a string instead of an array of strings.
