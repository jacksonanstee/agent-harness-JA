---
naem: unknown-field-skill
description: A skill with a misspelled name key, proving additionalProperties rejection.
version: 1.0.0
---

# Unknown Field

This skill has `naem` instead of `name`, which is both an unknown key
and a missing required field.
